<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chapters</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="font-serif">
    <h1 class="text-3xl underline text-center my-8"><%= thisPage.name %></h1>
    <div id="content" class="mx-8 mb-10 text-center">
      <%= thisPage.content %>
    </div>
    <div class="flex justify-evenly">
      <% if(prevPageId !== -1){ %>
      <a
        href="/page/<%= prevPageId %>"
        class="bg-green-500 border-2 px-1 rounded-sm border-black"
      >
        <- Previous
      </a>
      <% } %> <% if(!markedComplete) { %>
      <form action="/markComplete" method="post">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <input type="hidden" name="pageId" value="<%= thisPage.id %>" />
        <button class="bg-blue-600 border-2 px-1 rounded-sm border-black">
          Mark as complete
        </button>
      </form>
      <% } else { %>
      <div class="bg-green-600 border-2 px-1 rounded-sm border-black">
        Completed
      </div>
      <% } %> <% if(nextPageId !== -1) { %>
      <a
        href="/page/<%= nextPageId %>"
        class="bg-green-500 border-2 px-1 rounded-sm border-black"
      >
        Next ->
      </a>
      <% } %>
    </div>
  </body>
</html>
